@inject ServicioLogin servicioLogin
@inject NavigationManager NavigationManager
@inherits LayoutComponentBase

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main class="main-container">        
        <TopRow />
        <article class="no-padding">
            @Body
        </article> 
    </main>
    
</div>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>

<!-- 
¿Por qué podría no verse el usuario en el layout?
Causas posibles:
El usuario no se encuentra en la base de datos o la contraseña no coincide. (solucionado)
El layout se renderiza antes de que el usuario esté disponible. (solucionado: TopRow se suscribe a un evento)
El scope se pierde por recarga de página o navegación externa. (comportamiento esperado) 
--> 
